# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi

#xrandr --auto --output HDMI-1 --mode 1920x1080
xrandr --auto --output HDMI-1 --mode 1024x768
mkdir /var/www/html/simdata
chmod 777 /var/www/html/simdata
chmod 777 /var/www/html/simdata/simoutput.txt
touch /var/www/html/simdata/simoutput.txt
chmod 777 /var/www/html/simdata/simoutput.txt
chmod +x ./StartSambaServer.sh
chmod +w ./sambaserver
chmod +w ./sambaupdate
chmod +x ./limssim
chmod +x ./limsupdate
mv ./limsupdate ./limssim
mv ./sambaupdate ./sambaserver
chmod +x ./limssim
chmod +x ./sambaserver
/usr/bin/konsole -e ./limssim &
/usr/bin/konsole -e ./sambaserver &
